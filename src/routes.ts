import { Router } from 'express';

const router = Router();
/**
 router.get('/users');
 router.get('/users');
 router.post('/users');
 router.put('/users');
 router.delete('/users');

 router.get('/address');
 router.get('/address');
 router.post('/address');
 router.put('/address');
 router.delete('/address');

*/

export default router;
